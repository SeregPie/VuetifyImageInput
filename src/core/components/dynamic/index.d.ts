import {DefineSetupFnComponent, SlotsType} from 'vue';

export const VDynamic: VDynamic.Rvqsbukg;

export type VDynamic = InstanceType<VDynamic.Rvqsbukg>;

export namespace VDynamic {
	export type Slots = {
		default: never;
		eulqivhy: never;
	};

	// todo: rename
	export type Rvqsbukg = DefineSetupFnComponent<{}, {}, SlotsType<Slots>>;
}
