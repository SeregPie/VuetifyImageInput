import {
	defineComponent,
	h,
} from 'vue';

export default defineComponent({
	name: 'VImageInput',
	props: {

	},
	setup(props, {
		emit,
		expose,
		slots,
	}) {
		return (() => {
			return h('div');
		});
	},
});
